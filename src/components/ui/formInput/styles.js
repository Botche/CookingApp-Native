import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    container: {
        marginBottom: 10,
    },
    label: {

    },
    error: {
        color: "#f00",
    },
    input: {
        height: 40,
        borderWidth: 1,
        borderRadius: 5,
        padding: 10,
        width: '100%',
    },

});

export default styles;
