import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContenxt: 'center',
        alignItems: 'top',
    }
});

export default styles;
